# Claude Agent System - 完整项目系统

基于 SamuelQZQ 的实战架构，为 Claude Code 创建的长时间运行 Agent 系统。

## 🎯 核心流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        新建项目完整流程                                   │
└─────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────┐
    │  1. 项目初始化   │  ← 和 Agent 对话，获取项目信息
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │  2. 生成约束文件  │  ← 自动生成 PRD.md, architecture.md
    └────────┬────────┘     task.json, CLAUDE.md, rules.md 等
             │
             ▼
    ┌─────────────────┐
    │  3. 环境初始化   │  ← ./init.sh
    └────────┬────────┘
             │
             ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │                     4. 自动开发循环                                 │
    │  ┌───────────────────────────────────────────────────────────────┐  │
    │  │ 每个会话:                                                  │  │
    │  │   1. ./init.sh (启动服务器)                                │  │
    │  │   2. 读 task.json (选择下一个 passes:false 的任务)            │  │
    │  │   3. 实现功能                                             │  │
    │  │   4. 测试验证 (大改浏览器，小改 lint)                      │  │
    │  │   5. 更新 progress.txt                                    │  │
    │  │   6. git commit (包含 task.json 更新)                       │  │
    │  └───────────────────────────────────────────────────────────────┘  │
    └─────────────────────────────────────────────────────────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  5. 项目完成     │  ← 所有任务 passes: true
    └─────────────────┘
```

## 📁 必需的约束文件

```
project-root/
├── CLAUDE.md              # Agent 工作流程（核心约束）
├── PRD.md                 # 产品需求文档
├── architecture.md        # 架构设计文档
├── rules.md               # 编码规则（强约束）
├── testing.md             # 测试和验证规则
├── task.json              # 任务列表（单一真理来源）
├── progress.txt           # 进度日志（自动生成）
├── init.sh                # 初始化脚本
├── .env.example           # 环境变量模板
└── [项目代码目录]/
```

## 🚀 快速开始

```bash
# 1. 创建项目
mkdir my-project && cd my-project

# 2. 运行初始化对话
# 对 Agent 说：帮我初始化一个新项目

# 3. Agent 会通过对话获取信息，然后生成所有约束文件

# 4. 开始开发
./init.sh
claude -p
```
